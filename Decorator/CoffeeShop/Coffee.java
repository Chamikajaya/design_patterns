package com.learn.DesignPatterns.Behavioural.Decorator.CoffeeShop;

public interface Coffee {
    // This is the common interface which describes the main operations
    String getDescription();
    double getCost();
}
